apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-service
  namespace: openclaw-local
  labels:
    app: tenant-service
spec:
  replicas: 0
  selector:
    matchLabels:
      app: tenant-service
  template:
    metadata:
      labels:
        app: tenant-service
    spec:
      containers:
        - name: tenant-service
          image: openclaw/tenant-service:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          ports:
            - containerPort: 4002
          env:
            - name: PORT
              value: "4002"
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: openclaw-local
spec:
  type: ClusterIP
  selector:
    app: tenant-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002