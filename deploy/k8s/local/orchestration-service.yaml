apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestration-service
  namespace: openclaw-local
  labels:
    app: orchestration-service
spec:
  replicas: 0
  selector:
    matchLabels:
      app: orchestration-service
  template:
    metadata:
      labels:
        app: orchestration-service
    spec:
      containers:
        - name: orchestration-service
          image: openclaw/orchestration-service:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          ports:
            - containerPort: 4005
          env:
            - name: PORT
              value: "4005"
          readinessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: orchestration-service
  namespace: openclaw-local
spec:
  type: ClusterIP
  selector:
    app: orchestration-service
  ports:
    - name: http
      port: 4005
      targetPort: 4005