apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-service
  namespace: openclaw-local
  labels:
    app: policy-service
spec:
  replicas: 0
  selector:
    matchLabels:
      app: policy-service
  template:
    metadata:
      labels:
        app: policy-service
    spec:
      containers:
        - name: policy-service
          image: openclaw/policy-service:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          ports:
            - containerPort: 4003
          env:
            - name: PORT
              value: "4003"
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: policy-service
  namespace: openclaw-local
spec:
  type: ClusterIP
  selector:
    app: policy-service
  ports:
    - name: http
      port: 4003
      targetPort: 4003